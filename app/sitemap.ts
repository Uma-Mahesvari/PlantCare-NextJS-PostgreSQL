import type{MetadataRoute}from"next";import{blogPosts}from"../lib/blog";import{products}from"../lib/products";const base="https://plantcare-store.mugeshofc.chatgpt.site";export default function sitemap():MetadataRoute.Sitemap{const now=new Date();return[{url:base,lastModified:now,changeFrequency:"weekly",priority:1},{url:`${base}/products`,lastModified:now,changeFrequency:"daily",priority:.9},{url:`${base}/blog`,lastModified:now,changeFrequency:"weekly",priority:.8},{url:`${base}/about`,lastModified:now,changeFrequency:"monthly",priority:.5},{url:`${base}/contact`,lastModified:now,changeFrequency:"monthly",priority:.5},...blogPosts.map(p=>({url:`${base}/blog/${p.slug}`,lastModified:new Date(p.date),changeFrequency:"monthly" as const,priority:.7})),...products.map(p=>({url:`${base}/products/${p.id}`,lastModified:now,changeFrequency:"weekly" as const,priority:.6}))]}
