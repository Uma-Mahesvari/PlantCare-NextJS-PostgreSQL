import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {SiteHeader} from "../../../components/SiteHeader";import {blogPosts,getPost} from "../../../lib/blog";
export function generateStaticParams(){return blogPosts.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,p=getPost(slug);if(!p)return{};return{title:p.title,description:p.excerpt,alternates:{canonical:`/blog/${slug}`},openGraph:{title:p.title,description:p.excerpt,type:"article",publishedTime:p.date,images:[p.image]}}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const{slug}=await params,p=getPost(slug);if(!p)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:p.title,description:p.excerpt,image:`https://plantcare-store.mugeshofc.chatgpt.site${p.image}`,datePublished:p.date,dateModified:p.date,author:{"@type":"Organization",name:"PlantCare"},publisher:{"@type":"Organization",name:"PlantCare"}};return <div className="min-h-screen bg-[#fbfdf8] text-emerald-950"><SiteHeader/><main><article className="mx-auto max-w-3xl px-5 py-14"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><nav className="text-sm text-emerald-800"><Link href="/blog">Care tips</Link> / {p.category}</nav><p className="mt-8 text-xs font-bold uppercase tracking-[.25em] text-emerald-700">{p.category} · {p.readTime}</p><h1 className="mt-3 font-serif text-5xl leading-tight">{p.title}</h1><p className="mt-5 text-lg leading-8 text-emerald-950/65">{p.intro}</p><img src={p.image} alt="" className="mt-10 aspect-[16/8] w-full rounded-3xl object-cover"/>{p.sections.map(s=><section key={s.heading} className="mt-10"><h2 className="font-serif text-3xl">{s.heading}</h2><p className="mt-3 text-lg leading-8 text-emerald-950/75">{s.body}</p></section>)}<aside className="mt-12 rounded-3xl bg-emerald-950 p-8 text-white"><h2 className="font-serif text-3xl">Put these tips into practice</h2><p className="mt-3 text-white/70">Explore healthy plants and growing essentials selected for home gardeners.</p><Link href="/products" className="mt-6 inline-block rounded-full bg-lime-300 px-6 py-3 font-bold text-emerald-950">Shop plants & supplies</Link></aside></article></main></div>}
